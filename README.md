# AM_Node
Accounting Made Easy is a REST API created using Node.js to allow a user to manage their store. A user can create and retrieve customer profiles, stock and orders

The API make use a relational database using PostgreSQL and Sequelize
